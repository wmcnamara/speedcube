The files in this directory should be placed inside the `Speedcube_Linux` folder in order to install properly.\
Note to the user: you *may* need to run `chmod +x ./install.sh` to run the script properly.
